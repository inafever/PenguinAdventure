# 펭귄의 북극 모험 (Penguin's Arctic Adventure)

순수 클라이언트 사이드 HTML5 canvas 게임입니다. 빌드 단계, 번들러, 패키지 매니저, 자동화 테스트, 린트 설정이 없습니다.

- `index.html` — 페이지 구조와 HUD
- `style.css` — 스타일
- `game.js` — 게임 로직 전체 (vanilla JS, canvas 렌더링)
- `assets/` — 캐릭터/장애물 PNG 스프라이트 (penguin, bear, wolf, hunter)
- `assets/main-characters/` — 주인공 5종 프레임 애니메이션 (펭귄, 북극곰, 토끼, 여우, 물범)

## Cursor Cloud specific instructions

- 이 저장소는 정적 파일만으로 이루어져 있어 설치할 의존성이 없습니다. `python3`(베이스 이미지에 이미 설치됨)만 있으면 됩니다.
- 게임은 반드시 로컬 HTTP 서버로 열어야 합니다. `file://`로 열면 canvas가 `assets/*.png` 스프라이트를 로드하지 못해 캐릭터가 그려지지 않습니다. 서버 실행: `python3 -m http.server 8765` (저장소 루트에서). 이후 `http://127.0.0.1:8765/index.html` 접속.
- 진행 상태(닉네임, 최고 점수, 코인, 낮/밤 테마, 상점 구매)는 브라우저 `localStorage`에 저장됩니다. 서버 쪽 상태나 DB는 없습니다. 깨끗한 상태로 테스트하려면 브라우저 저장소를 비우세요.
- 첫 플레이 시 닉네임 입력 오버레이가 먼저 뜨고, 이어서 캐릭터 선택(펭귄/북극곰/토끼/여우/물범)을 고른 뒤에야 "시작하기"로 게임을 시작할 수 있습니다. 선택은 `localStorage` 키 `penguin-char`에 저장됩니다. 이후 헤더 **캐릭터** 버튼(전체화면에서는 🐧)으로 바꿀 수 있습니다.
- 주인공 스프라이트는 `assets/main-characters/`의 `right` 프레임을 씁니다. 펭귄 프레임은 모두 188×264라서 그대로 그립니다(자르면 발이 흔들립니다). 북극곰·토끼·여우·물범은 프레임마다 파일 크기가 달라서, 여백을 뺀 실제 그림만 72×84 상자에 contain합니다. 선택 창 썸네일은 `walk_right_1` 정지 컷입니다.
- 프레임은 한 번에 122장을 받지 않습니다. 먼저 썸네일 5장(`walk_right_1`)과 고른(또는 기본 펭귄) 캐릭터 애니만 받고, 나머지는 백그라운드에서 조금씩 받습니다. 시작 시 `getImageData`로 전 프레임을 재지 않습니다.
- 조작: 스페이스바 / ↑ / 마우스 클릭 / 터치로 점프. 게임은 자동으로 우측으로 달립니다.
- 5단계 이후에는 플레이 시간 1분마다 낮/밤이 자동으로 바뀝니다. 상점·캐릭터·랭킹·닉네임 창을 열거나 다른 탭으로 나가면 게임과 점수 시계가 같이 멈춥니다.
- 온라인 랭킹은 점수/코인 모두 고유 닉네임을 제한 없이 보여 줍니다. 목록이 길면 랭킹 창 안에서 스크롤합니다. 10만점 초과 저장은 라이브 Supabase `submit_score` 상한을 `config.js` 안내 SQL로 올려야 합니다.
- 수정 후 데모 화면 녹화는 하지 마세요. 사용자에게 직접 플레이해서 확인해 달라고 안내하면 됩니다.
